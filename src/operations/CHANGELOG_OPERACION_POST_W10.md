# Changelog Operacion Post-W10

## 2026-05-07T11:24:55-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `src/operations/frozen_profiles.py` | `creacion` | Se creo un registro externo de perfiles congelados y comandos derivados desde `metadata_run.json` y `parametros_run.json` para `E1_v5_clean`, `E2_v3_clean`, `E3_v2_clean`, `E5_v4_clean`, `E7_v3_clean` y `E9_v2_clean`. | Evitar cambiar runners existentes y fijar la operacion sobre artefactos canonicos reales. | Permite invocar perfiles congelados sin reescribir ni editar `src/modeling/`. | `si` | Validar corrida real en entorno de modelado. |
| `src/operations/frozen_inference.py` | `creacion` | Se creo una capa externa para construir predicciones pendientes y bases derivadas de `E9` sin tocar la logica de los runners originales. | Separar operacion minima y registro de emisiones del codigo de modelado existente. | Hace posible construir emisiones post-W10 desde perfiles congelados. | `si` | Validar emision real y revisar costos de runtime. |
| `src/operations/build_emission_registry_post_w10.py` | `creacion` | Se creo un constructor externo del registro formal de emisiones para correr con el entorno de modelado. | Evitar que la capa de control dependa de `sklearn`, `catboost` o `prophet`. | Separa control operativo y construccion de emisiones manteniendo el modelado intacto. | `si` | Probar corrida real completa. |
| `src/operations/run_operacion_minima_post_w10.py` | `creacion` | Se creo un runner externo para backfill NLP post-W10, refresco de perfiles congelados, refresco de tabla curada `E9` y disparo del constructor de emisiones. | Dejar un punto de entrada operativo sin tocar scripts existentes. | Da una secuencia controlada y trazable para `2026-W11 -> semana vigente`. | `si` | Validar corrida real completa. |
| `src/operations/run_operacion_minima_post_w10.py` y `src/operations/README_operacion_minima_post_w10.md` | `aclaracion_metodologica` | Se agregaron overrides explícitos `--ops-python` y `--modeling-python` porque el `MODELING_PYTHON` default detectado en este workspace apunta a un entorno inexistente. | Evitar que la operacion nueva dependa de un `.env` stale y falle antes de tocar datos. | Mejora operabilidad real sin tocar la configuracion vieja. | `si` | Confirmar ruta definitiva del entorno de modelado en el entorno del usuario. |
| `src/operations/README_operacion_minima_post_w10.md` | `creacion` | Se documento la operacion minima controlada post-W10, la frontera W10/W11, el rol de `E1_v5_clean` y `E9_v2_clean`, y la estructura del registro de emisiones. | Dejar trazabilidad y manual operativo sin reescribir documentacion previa. | Reduce ambiguedad operativa y fija el alcance real del wrapper. | `si` | Mantenerlo actualizado si cambia el corte operativo. |
| `experiments/prompts/35_operacion_minima_post_w10/01_operacion_minima_controlada_post_w10.md` | `creacion` | Se guardo el prompt de trabajo de esta tarea. | Trazabilidad del encargo operativo. | Preserva la especificacion usada para la capa post-W10. | `si` | Ninguna. |
| `experiments/prompts/README.md` | `modificacion` | Se agrego referencia al prompt de operacion minima post-W10. | Mantener indexado el catalogo de prompts del repo. | Mejora navegacion y trazabilidad documental. | `si` | Ninguna. |

## Decisiones explicitamente no tomadas

| Tema | Decision | Motivo | Riesgo abierto |
| --- | --- | --- | --- |
| `src/modeling/` | No se modifico. | Restriccion explicita del usuario y congelamiento metodologico. | La operacion depende de que los runners actuales sigan siendo compatibles con sus artefactos canonicos. |
| Scripts existentes en `src/operations/` | No se movieron ni se reescribieron. | Restriccion explicita del usuario. | La documentacion vieja que menciona `E10` sigue existiendo fuera de esta capa nueva. |
| Corrida real completa | No se ejecuto en esta tarea. | Se priorizo endurecimiento controlado, compilacion y `dry-run` sin tocar datos ni modelos. | Falta validar el registro final con una corrida materializada. |
| `MODELING_PYTHON` default | No se corrigio en la capa vieja. | Restriccion de no alterar scripts existentes; el ajuste se encapsulo en el wrapper nuevo. | Si no se pasa override ni se corrige `.env`, la corrida real seguira fallando por ruta inexistente. |

## 2026-05-07T12:11:00-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `src/operations/run_operacion_minima_post_w10.py` | `correccion_operativa` | Se corrigio la reconstruccion de `tabla_maestra_experimentos_radar_e9_curada.xlsx` para que corra con `modeling_python` y no con `ops_python`. | Evitar fallas por dependencias de modelado ausentes en el entorno operativo. | Hace consistente la separacion de entornos sin tocar scripts existentes. | `si` | Validar corrida real completa. |
| `src/operations/run_operacion_minima_post_w10.py` | `ajuste_documental_operativo` | Se agrego al `manifest.json` el mapeo `canonical_profiles` con `canonical_run_dir`, `metadata_path`, `parameters_path` y `runner_module`. | Fijar por corrida los artefactos exactos del congelamiento operativo. | Mejora trazabilidad de la operacion sin modificar modelos ni runners. | `si` | Revisar el manifest tras la primera corrida real. |
| `src/operations/README_operacion_minima_post_w10.md` | `ajuste_documental` | Se documento la separacion de entornos `ops_python` vs `modeling_python` y el contenido canonico adicional del `manifest.json`. | Alinear la documentacion con la implementacion real endurecida. | Reduce ambiguedad operativa y facilita auditoria. | `si` | Mantener sincronizado con futuros ajustes del wrapper externo. |

## 2026-05-08T09:00:00-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `src/operations/README_operacion_minima_post_w10.md` | `ajuste_documental` | Se dejo explicito que la capa post-W10 no esta integrada dentro de `pipeline_orchestrator.py`, `run_radar_pipeline.py` ni `gui_radar_pipeline.py`, y que opera como entrypoint externo. | Cerrar ambiguedad sobre el alcance real de la implementacion. | Evita confundir esta capa nueva con la UI/orquestacion previa. | `si` | Si en el futuro se desea integracion, hacerlo como decision aparte y registrada. |

## 2026-05-08T06:10:00-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `src/operations/run_operacion_minima_post_w10.py` | `ajuste_operativo` | Se habilito `main(argv)` para que la operación post-W10 pueda ser invocada desde el entrypoint canónico sin shelling-out adicional. | Integrar la operación a `run_radar_pipeline.py` conservando el wrapper como implementación base. | Permite reutilizar la lógica post-W10 desde CLI y GUI existentes. | `si` | Mantener la firma estable si se añaden opciones nuevas. |
| `src/operations/run_radar_pipeline.py` | `integracion_operativa` | Se agregó `--operation-profile post_w10_controlled` y flags asociados para delegar la operación mínima post-W10 desde el entrypoint operativo único. | Inyectar la operación post-W10 dentro del launcher canónico sin tocar `src/modeling/`. | Unifica la entrada de operación semanal normal y post-W10 bajo el mismo CLI. | `si` | Validar corrida real completa desde el entrypoint integrado. |
| `src/operations/gui_radar_pipeline.py` | `integracion_ui` | Se agregó el preset `Operación mínima post-W10` con campos dedicados para rango de semanas, `operation_id`, comentario y override de `modeling_python`. | Exponer la operación integrada desde la UI existente sin reutilizar controles ambiguos del pipeline semanal. | Permite disparar el perfil post-W10 desde la GUI con previsualización del comando correcto. | `si` | Probar interacción gráfica manual en entorno con display. |
| `src/operations/README_operacion_minima_post_w10.md` | `ajuste_documental` | Se actualizó la documentación para reflejar que la operación post-W10 ya está integrada a `run_radar_pipeline.py` y a `gui_radar_pipeline.py`, manteniendo `run_operacion_minima_post_w10.py` como wrapper subyacente. | Alinear la documentación con el estado real del código. | Elimina la ambigüedad previa sobre “solo externo”. | `si` | Revisar tras la primera corrida real integrada. |
| `src/operations/README.md` | `ajuste_documental` | Se documentó el perfil integrado `post_w10_controlled` y su exposición en la GUI. | Mantener el README general de operaciones sincronizado con la nueva capacidad integrada. | Facilita el uso correcto del entrypoint canónico. | `si` | Mantener sincronía si cambian flags o presets. |

## 2026-05-08T06:15:00-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `src/operations/run_operacion_minima_post_w10.py` | `ajuste_operativo` | Se amplió el wrapper para respetar selección de etapas entre `extraction`, `preprocessing`, `nlp` y `modeling`, ejecutando el tramo semanal solo hasta la frontera pedida y corriendo modelado/emisiones únicamente si `to-stage=modeling`. | Hacer que la operación post-W10 use la semántica de selección de etapas esperada por el launcher. | Permite correr desde extracción o desde NLP según la selección explícita del usuario. | `si` | Validar corrida real de al menos dos combinaciones de etapas. |
| `src/operations/run_radar_pipeline.py` | `integracion_operativa` | Se habilitó `--operation-profile post_w10_controlled` para aceptar `--from-stage/--to-stage` y mapearlos al wrapper post-W10. | Alinear la integración con la lógica existente del launcher y evitar un perfil “todo o nada”. | La CLI integrada ahora respeta la selección de etapas del usuario. | `si` | Mantener restricción a rango `extraction -> modeling`. |
| `src/operations/gui_radar_pipeline.py` | `integracion_ui` | Se habilitó que el preset `Operación mínima post-W10` use los controles `From Stage / To Stage` y construya el comando integrado correspondiente. | Hacer consistente la UI con la expectativa de selección de etapas. | Al dar play desde la GUI, el perfil post-W10 ya no arranca forzosamente en NLP. | `si` | Probar manualmente en entorno con display. |
| `src/operations/README_operacion_minima_post_w10.md` y `src/operations/README.md` | `ajuste_documental` | Se documentó que la operación integrada post-W10 ya respeta la selección de etapas `extraction -> modeling`. | Evitar contradicción entre comportamiento real y documentación. | Reduce ambigüedad operativa. | `si` | Mantener sincronía con futuras extensiones del perfil. |

## 2026-05-08T07:35:00-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `experiments/prompts/35_operacion_minima_post_w10/03_correccion_estricta_separacion_entrenamiento_inferencia_post_w10.md` | `creacion` | Se guardó el prompt específico de corrección estricta `fit/predict` para la operación post-W10. | Trazabilidad del encargo correctivo y de la validación metodológica asociada. | Preserva la especificación usada para endurecer la separación entrenamiento/inferencia. | `si` | Ninguna. |
| `experiments/prompts/README.md` | `modificacion` | Se indexó el prompt de corrección estricta de separación entrenamiento/inferencia. | Mantener el catálogo de prompts sincronizado con la nueva tarea ejecutada. | Mejora la auditabilidad documental. | `si` | Ninguna. |
| `src/operations/frozen_inference_assets.py` | `creacion` | Se creó la capa de bootstrap/carga de paquetes `predict-only`, con corte histórico explícito en `2026-W10` y serialización de `E1_v5_clean` por horizonte. | Separar de forma dura la preparación del paquete congelado respecto a la operación post-W10. | Permite que `E1_v5_clean` cargue artefactos de inferencia sin recalcular tuning ni usar semanas nuevas para entrenar. | `si` | Extender el patrón a otros perfiles si se congelan artefactos suficientes. |
| `src/operations/bootstrap_frozen_inference_assets.py` | `creacion` | Se creó un bootstrap explícito para materializar paquetes `predict-only` fuera del flujo operativo semanal. | Evitar que `post_w10_controlled` construya artefactos de inferencia mientras corre emisiones. | La preparación del paquete congelado queda trazable, manual y separada de la ejecución operativa. | `si` | Automatizar solo si se mantiene la separación dura respecto a la operación. |
| `src/operations/frozen_inference.py` | `modificacion_estructural` | Se reemplazó la capa anterior, que volvía a ejecutar `fit()`, por una ruta estricta `predict-only` que carga modelos serializados y valida contrato de features. | Eliminar definitivamente el reentrenamiento implícito dentro de la operación post-W10. | `post_w10_controlled` ya no reutiliza silenciosamente la ruta experimental para `E1_v5_clean`. | `si` | Implementar soporte equivalente para `E9` solo cuando exista paquete congelado suficiente. |
| `src/operations/build_emission_registry_post_w10.py` | `modificacion_operativa` | El registro de emisiones pasó a consumir la ruta `predict-only`, usar el paquete congelado de `E1`, y marcar `E9_v2_clean` como `blocked` cuando no existe paquete suficiente. | Materializar inferencia operativa sin refresh experimental de runs. | El registro final ya distingue perfiles listos vs bloqueados y preserva trazabilidad de artefactos usados. | `si` | Completar soporte real de `E9` o mantener bloqueo explícito. |
| `src/operations/run_operacion_minima_post_w10.py` | `blindaje_operativo` | Se eliminó el refresh experimental de runners/modelos del tramo `modeling` y se delegó exclusivamente al constructor de emisiones `predict-only`. | Impedir por código que `post_w10_controlled` vuelva a ejecutar rutas de entrenamiento. | El wrapper operativo deja de llamar silenciosamente a `run_e1_ridge_clean`, `run_e9_stacking` y reconstrucciones ligadas a `fit()` durante post-W10. | `si` | Mantener este blindaje si se añaden perfiles nuevos. |
| `src/operations/README_operacion_minima_post_w10.md` y `src/operations/README.md` | `ajuste_documental` | Se actualizó la documentación para reflejar el estado real: `E1` validado en `predict-only`, `E9` aún incompleto. | Alinear documentación con la corrección técnica y la validación material ejecutada. | Reduce riesgo de interpretar la capa como completamente lista cuando aún hay un bloqueo explícito en `E9`. | `si` | Actualizar cuando `E9` tenga paquete congelado propio. |

## 2026-05-08T07:58:00-06:00

| Archivo | Tipo | Descripcion | Motivo | Impacto esperado | Compatibilidad hacia atras | Accion posterior |
| --- | --- | --- | --- | --- | --- | --- |
| `src/operations/frozen_inference_assets.py` | `modificacion_estructural` | Se extendió el bootstrap `predict-only` para `E2_v3_clean`, `E3_v2_clean`, `E5_v4_clean`, `E7_v3_clean` y `E9_v2_clean`, incluyendo contrato de ensamblado por horizonte y serialización del meta-modelo de `E9`. | Cerrar el hueco que dejaba a `E9` fuera de la operación post-W10 por falta de paquete congelado completo. | La capa operativa ya puede publicar paquetes congelados para el campeón, las bases del stacking y el referente de riesgo sin tocar `src/modeling/`. | `si` | Mantener sincronizado el contrato si cambia el benchmark congelado. |
| `src/operations/frozen_inference.py` | `modificacion_operativa` | Se generalizó la ruta `predict-only` para consumir paquetes base por perfil y se habilitó `E9_v2_clean` como meta-modelo que ensambla predicciones congeladas de `E1/E2/E3/E5/E7`. | Evitar cualquier fallback silencioso a runners experimentales cuando se piden emisiones del referente de riesgo. | `post_w10_controlled` ahora puede emitir predicciones pendientes de `E9` con `predict()` solamente. | `si` | Vigilar compatibilidad numérica si se vuelven a publicar paquetes congelados. |
| `src/operations/bootstrap_frozen_inference_assets.py` | `ajuste_operativo` | Se amplió el CLI de bootstrap para reflejar soporte operativo de `E1/E2/E3/E5/E7/E9`. | Alinear la ayuda del entrypoint con la cobertura real de paquetes congelados. | Reduce ambigüedad al materializar activos de inferencia. | `si` | Ninguna. |
| `src/operations/build_emission_registry_post_w10.py` | `modificacion_operativa` | El registro de emisiones dejó de marcar `E9` como bloqueado y ahora persiste también su `package_root` cuando el paquete meta-modelo está listo. | Cerrar la trazabilidad de emisiones del referente de riesgo una vez completado su paquete congelado. | La bitácora operativa refleja `E1` y `E9` como perfiles `ready` y deja snapshots por horizonte para ambos. | `si` | Auditar una corrida más amplia `W11 -> semana vigente` si se desea backfill completo. |
| `src/operations/README_operacion_minima_post_w10.md` y `src/operations/README.md` | `ajuste_documental` | Se actualizó la documentación para reflejar que la ruta `predict-only` ya quedó validada materialmente para `E1_v5_clean` y `E9_v2_clean`. | Evitar documentación stale que siguiera reportando a `E9` como incompleto. | La capa operativa queda descrita conforme a su estado real de validación. | `si` | Mantener sincronía si cambian artefactos o contratos congelados. |
| `artifacts/operations/frozen_inference_profiles/E2_v3_clean/`, `E3_v2_clean/`, `E5_v4_clean/`, `E7_v3_clean/`, `E9_v2_clean/` | `creacion_artefactos` | Se materializaron paquetes `predict-only` por horizonte a partir de las corridas canónicas y del corte histórico `2026-W10`. | Publicar activos de inferencia suficientes para operar `E9` sin reentrenamiento post-W10. | La operación ya puede cargar artefactos serializados en lugar de rehacer `fit()`. | `si` | Regenerar solo si cambia el benchmark congelado o el criterio de corte histórico. |
| `artifacts/operations/post_w10/post_w10_predict_only_e1_e9_validation_20260508_0758/` | `validacion_material` | Se ejecutó una corrida real `modeling -> emisiones` con `post_w10_controlled` y se registraron emisiones nuevas para `E1` y `E9`. | Demostrar materialmente que la separación `train/predict` ya funciona también para el referente de riesgo. | Queda evidencia auditable de que la operación post-W10 emite predicciones nuevas sin refresh experimental de runs. | `si` | Si se requiere, extender la validación al tramo completo `nlp -> modeling` o `extraction -> modeling`. |

## Decisiones explicitamente no tomadas tras la correccion fit/predict

| Tema | Decision | Motivo | Riesgo abierto |
| --- | --- | --- | --- |
| `E9_v2_clean` | No se declaró listo ni se forzó una ruta parcial que rehaga fitting. | No existe todavía paquete congelado suficiente del ensamblado/meta-modelo y de sus dependencias para operar sin `fit()`. | La operación post-W10 queda validada materialmente solo para `E1` y no para el referente de riesgo. |
| `src/modeling/` | No se modificó para serializar modelos o exponer una API de inferencia. | Restricción explícita del usuario y congelamiento metodológico. | La congelación de otros perfiles requiere seguir construyendo paquetes desde la capa operativa o abrir una tarea específica de publicación de artefactos. |
